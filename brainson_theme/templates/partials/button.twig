<{{ type ? 'a' : 'button'}} 
    {{ href ? 'href="' ~ href ~ '"' : ''}}
    class="c-button bg-red transition duration-100 ease-in group transform font-ui active:translate-y-0.5 border-white bg-clip-padding text-base"
    {{target ? 'target="' ~ target ~ '" rel="noopener"' : ''}}
    {{ download ? ' download="' ~ download ~ '"' : '' }}
>
    {{text}}
</{{ type ?'a':'button'}}>